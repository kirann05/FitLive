"use client";
import {useState} from 'react';
import {Minus,Plus} from 'lucide-react';
export function SetStepper({label,value,step,min,max,onChange}:{label:string;value:number|null;step:number;min:number;max:number;onChange:(n:number)=>void}){const [editing,setEditing]=useState(false),[draft,setDraft]=useState("");return <div><span className="eyebrow">{label}</span><div className="stepper"><button type="button" aria-label={`Decrease ${label}`} disabled={value===null||value<=min} onClick={()=>onChange(Math.max(min,(value??min)-step))}><Minus/></button>{editing?<input autoFocus aria-label={label} inputMode="decimal" type="number" min={min} max={max} step={step} value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();e.currentTarget.blur();}}} onBlur={()=>{if(draft.trim()&&Number.isFinite(Number(draft)))onChange(Math.min(max,Math.max(min,Number(draft))));setEditing(false);}}/>:<button type="button" className="stepper-value" onClick={()=>{setDraft(value===null?"":String(value));setEditing(true);}}>{value??'Set load'}</button>}<button type="button" aria-label={`Increase ${label}`} disabled={value!==null&&value>=max} onClick={()=>onChange(Math.min(max,(value??0)+step))}><Plus/></button></div></div>;}
